version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: nimbus-postgres
    environment:
      POSTGRES_DB: nimbus_autopilot
      POSTGRES_USER: nimbus_user
      POSTGRES_PASSWORD: ${DB_PASSWORD:-change_me_in_production}
      POSTGRES_INITDB_ARGS: "-E UTF8"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nimbus_user -d nimbus_autopilot"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: nimbus-api
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: nimbus_autopilot
      DB_USER: nimbus_user
      DB_PASSWORD: ${DB_PASSWORD:-change_me_in_production}
      API_KEY: ${API_KEY:-change_me_in_production}
      PORT: 5000
      FLASK_DEBUG: "False"
    ports:
      - "5000:5000"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  dashboard:
    build:
      context: ./dashboard
      dockerfile: Dockerfile
      args:
        REACT_APP_API_URL: ${REACT_APP_API_URL:-http://localhost:5000}
        REACT_APP_API_KEY: ${API_KEY:-change_me_in_production}
    container_name: nimbus-dashboard
    ports:
      - "80:80"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  postgres_data:
    name: nimbus_postgres_data
